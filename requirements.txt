# InsightFlow — Business Analytics Automation Platform
# Core runtime dependencies (all required)

streamlit>=1.40.0
pandas>=2.2.0
numpy>=1.26.0
plotly>=5.22.0
sqlalchemy>=2.0.0
scikit-learn>=1.4.0
openpyxl>=3.1.0
reportlab>=4.1.0
faker>=24.0.0
python-dotenv>=1.0.0

# Optional — improves file-upload encoding detection; falls back gracefully
# without it.
chardet>=5.2.0

# Optional — enables PowerPoint (.pptx) report export.
python-pptx>=0.6.23

# Optional — enables embedding static chart images inside PDF reports.
kaleido>=0.2.1

# --- Optional AI providers -------------------------------------------------
# InsightFlow works fully offline. Install ONE of these (and set the
# matching API key) to enable AI-narrated insights and chat. See
# docs/CONFIGURATION.md.
# anthropic>=0.40.0
# openai>=1.50.0
# google-generativeai>=0.8.0

# --- Testing -----------------------------------------------------------
pytest>=8.0.0
