"""Data engineering pipelines: ingestion, cleaning, validation, profiling
and transformation. Each module is a pure-function toolkit operating on
pandas DataFrames — no Streamlit or session-state imports here, so every
pipeline is independently unit-testable.
"""
